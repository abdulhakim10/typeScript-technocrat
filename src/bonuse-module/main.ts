import  * as Methods  from "./module";
import average from "./module";

const add1 = (param1: number, param2: number, param3: number): number => {
    return param1 + param2 + param3;
};

const res = Methods.add1(4,6);

const re = average.add1(3,4);